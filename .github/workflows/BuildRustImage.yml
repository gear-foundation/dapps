name: Build and Use Builder Image

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release image version'
        required: true
        default: '1.2.0'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the GitHub Container Registry
        uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Rust Build image
        uses: docker/build-push-action@master
        with:
          push: true
          context: scripts/docker-rust
          tags: ghcr.io/${{ github.repository }}:${{ inputs.version }}

  build-project:
    needs: build-and-push-image
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository }}:${{ inputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        working-directory: contracts
        run: 'cargo build --release --workspace;'

      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v4
        with:
          name: wasm
          path: |
            contracts/target/wasm32-unknown-unknown/release/*.*.wasm
            contracts/target/wasm32-unknown-unknown/release/*.meta.txt

      - name: Update the nightly tag
        if: github.ref == 'refs/heads/sf-release'
        run: |
          git tag nightly
          git push -f origin nightly

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          name: ${{ github.ref == 'refs/heads/sf-release' && 'Nightly' || github.ref_name }}
          tag_name: ${{ github.ref == 'refs/heads/sf-release' && 'nightly' || github.ref_name }}
          prerelease: ${{ github.ref == 'refs/heads/sf-release' }}
          files: |
            contracts/target/wasm32-unknown-unknown/release/*.*.wasm
            contracts/target/wasm32-unknown-unknown/release/*.meta.txt

      - name: Upload the GitHub Pages artifact
        if: github.ref == 'refs/heads/master'
        uses: actions/upload-pages-artifact@v3
        with:
          path: contracts/target/doc
