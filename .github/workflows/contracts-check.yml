name: Contracts CI

on:
  pull_request:
    branches: [master]
  workflow_call:

env:
  CARGO_TERM_COLOR: always

jobs:
  all:
    name: All
    runs-on: Ubuntu_Large_1
    defaults:
      run:
        working-directory: contracts
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: CI
        run: cargo xtask ci
