export const LOCAL_STORAGE = {
  ACCOUNT: 'account',
}

export const ENV = {
  NODE: process.env.REACT_APP_NODE_ADDRESS as string,
}

export const ROUTES = {
  HOME: '/',
  GAME: '/battle',
  TEST: '/test',
  NOTFOUND: '*',
}
