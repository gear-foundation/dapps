export type UseFetchVoucherProps = {
  programId: `0x${string}`;
  backendAddress: string;
  voucherLimit?: number;
};

export type InitVoucher = {
  programId: `0x${string}`;
  backendAddress: string;
  voucherLimit?: number;
};
