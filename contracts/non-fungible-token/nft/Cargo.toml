[package]
name = "nft"
version = "0.1.0"
authors = ["Gear Technologies"]
edition = "2021"

[dependencies]
gstd = { git = "https://github.com/gear-tech/gear.git", features = ["debug"] }
primitive-types = { version = "0.11.1", default-features = false, features = ["scale-info"]}
codec = { package = "parity-scale-codec", version = "3.1.2", default-features = false, features = ["derive"] }
scale-info = { version = "2.0.1", default-features = false, features = ["derive"] }
gear-lib = { git = "https://github.com/gear-dapps/gear-lib.git", branch = "feature/delegated-approve" }
nft-io = { path = "io" }
gear-lib-derive = { git = "https://github.com/gear-dapps/gear-lib.git", branch = "feature/delegated-approve" }

[dev-dependencies]
gtest = { git = "https://github.com/gear-tech/gear.git" }
hex-literal = "0.3.4"
sp-core = { version = "6.0.0", git = "https://github.com/gear-tech/substrate.git", branch = "gear-stable" }

[build-dependencies]
gear-wasm-builder = { git = "https://github.com/gear-tech/gear.git" }
