import styles from './calc.module.scss';
import clsx from 'clsx';

export const LauncheCalc = () => {
  return (
    <div className={styles.lcalc}>
      *** Calculation block ***
      <p>here is calculation / block /</p>
    </div>
  );
};
