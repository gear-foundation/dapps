[package]
name = "dex-pair"
version = "0.1.0"
edition = "2021"
license = "MIT"
authors = ["Gear Technologies"]

[dependencies]
gstd = { git = "https://github.com/gear-tech/gear.git", features = ["debug"] }
codec = { package = "parity-scale-codec", version = "3.1.2", default-features = false, features = ["derive", "full"] }
scale-info = { version = "2.0.1", default-features = false, features = ["derive"] }
primitive-types = { version = "0.11.1", default-features = false, features = ["scale-info"]}
dex-pair-io = { path = "../pair/io" }
dex-factory-io = { path = "../factory/io" }

gear-lib = { git = "https://github.com/gear-dapps/gear-lib.git", tag = "0.3.0" }
gear-lib-derive = { git = "https://github.com/gear-academy/gear-lib.git", tag = "0.3.0" }
num = { version = "0.4", default-features = false }
ft-io = { git = "https://github.com/gear-dapps/fungible-token.git", tag = "0.1.0" }

[dev-dependencies]
gtest = { git = "https://github.com/gear-tech/gear.git"}

[build-dependencies]
gear-wasm-builder = { git = "https://github.com/gear-tech/gear.git" }
